services:
  - name: goal-stream-api
    type: web
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    healthCheckPath: /api/v1/health
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: DATABASE_URL
        fromDatabase:
          name: goal-stream-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: SPORTS_PROVIDER
        value: sportmonks
      - key: SPORTMONKS_BASE_URL
        value: https://api.sportmonks.com/v3/football
      - key: SPORTMONKS_API_TOKEN
        sync: false
      - key: GNEWS_API_KEY
        sync: false

  - name: goal-stream-web
    type: web
    runtime: docker
    plan: free
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: .
    healthCheckPath: /ar
    envVars:
      - key: INTERNAL_API_BASE_URL
        fromService:
          type: web
          name: goal-stream-api
          property: hostport

databases:
  - name: goal-stream-db
    plan: free
    databaseName: goal_stream
